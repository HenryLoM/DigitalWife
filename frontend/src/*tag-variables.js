// HTML id tagging
const HTML_TAG = {
    titleInputField:    "title-input",   // Name of the chat
    chattingWindow:     "chat-window",   // Block with the other div "chat-content" and mini-frame
    chattingContent:    "chat-content",  // All messages
    chattingInputField: "user-input",    // Textarea where user texts the prompt
    popupWindow:        "editor-popup",  // Popup window with the input field, two buttons, and character counter
    popupInputField:    "editing-text",  // Popup's input field
    popupCharCounter:   "char-counter",  // Popup's character counter
};

// CSS id tagging
const CSS_TAG = {
    messageFramePreset: "message-frame-preset",     // Message frame styling preset
    tooltipPreset:      "tooltip-preset",           // Tooltip styling preset
    frameButtonRow:     "frame-button-row-preset",  // Row with buttons in frame
    editButtonRow:      "edit-button-row-preset",   // Row with edit buttons
};
